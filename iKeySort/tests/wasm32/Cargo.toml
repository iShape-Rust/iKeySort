[package]
name = "wasm32_regression"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
i_key_sort = { path = "../.." }

[profile.release]
panic = "abort"
